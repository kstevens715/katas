var expect = require('chai').expect;
var Solution = require('../solution');

describe('Solution', function() {
  it('README example', function() {
    solution = new Solution;
    expect(solution.run()).to.equal(null);
  });
});
